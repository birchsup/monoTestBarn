# OPEN_QUESTIONS_TASKS_STATUS

Статус задач из `docs/OPEN_QUESTIONS_TASKS.md` по фактическому состоянию кода.

Легенда:
- `[done]` — выполнено полностью по требованиям документа.
- `[partial]` — реализовано частично, но DoD не закрыт.
- `[todo]` — не реализовано.

## 1. Сделать путь к миграциям в интеграционных тестах переносимым
- Статус: `[partial]`
- Почему:
  - Абсолютный путь убран.
  - Добавлено переносимое определение `db/migrations`: через `TEST_MIGRATIONS_PATH`, поиск от `cwd`, поиск от пути тестового файла.
  - Но DoD требует подтверждения прохождения интеграционных тестов в чистом окружении; в текущей среде это не подтверждено, потому что запуск тестов упирается в недоступный Docker daemon.
  - Комментарий в коде есть, но полноценная инструкция в README для этого сценария не оформлена.

## 2. Синхронизировать API-контракт получения test case между роутами и тестами
- Статус: `[partial]`
- Почему:
  - Для получения test case выбран и реализован `GET /testcases/{id}`.
  - Роутинг, handler и интеграционные тесты используют один и тот же path-param контракт.
  - Добавлены проверки на некорректный формат id, отсутствие path route, несуществующий id, нулевой и отрицательный id.
  - Статус оставлен `partial` только потому, что полный integration run не был подтверждён в текущем окружении без Docker.

## 3. Проверить корректность конфигурации CORS-заголовков в `main.go`
- Статус: `[partial]`
- Почему:
  - Дублирование `AllowedHeaders(...)` уже убрано: используется один явный список заголовков.
  - Но нет зафиксированного проверочного сценария или ссылки на подтверждение поведения библиотеки.
  - Дополнительно `PATCH` отсутствует в `AllowedMethods`, что мешает browser-flow для обновления run-case.

## 4. Определить и зафиксировать статус функциональности `test_runs`
- Статус: `[partial]`
- Почему:
  - `test_runs` уже явно активированы в коде и доступны через публичные handlers/routes.
  - Минимальный scope по create/list/get и patch статусов реализован.
  - Но противоречия между кодом, общим API-контрактом и планом всё ещё остаются: не закрыты error contract, общий REST cleanup и часть DoD вокруг MVP.

## 5. Подготовить официальный README по запуску и миграциям
- Статус: `[partial]`
- Почему:
  - README в репозитории есть.
  - В нём описан базовый запуск через Docker и запуск тестов.
  - Но не покрыты в требуемой полноте `prerequisites`, env, отдельный workflow миграций, различия dev/test и сценарий для нового участника команды без чтения кода.
